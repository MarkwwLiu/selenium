[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    smoke: 冒煙測試（快速驗證核心功能）
    regression: 迴歸測試（完整功能驗證）
    positive: 正向測試（合法輸入，預期成功）
    negative: 反向測試（非法輸入，預期失敗）
    boundary: 邊界測試（邊界值，驗證臨界行為）
